<Window
    x:Class="Caelum.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="600"
    Height="700"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    WindowStyle="None"
    Background="Transparent"
    AllowsTransparency="True"
    ShowInTaskbar="False">

    <Border Background="{DynamicResource ThemePaperBrush}" CornerRadius="18" Padding="24" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1">
        <ScrollViewer Style="{StaticResource SleekScrollViewer}" VerticalScrollBarVisibility="Hidden" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border x:Name="SettingsMarginRail"
                            Grid.Column="0"
                            Background="{DynamicResource ThemeMarginBrush}"
                            CornerRadius="2"/>

                    <StackPanel Grid.Column="2">
                        <TextBlock x:Name="TitleTextBlock" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource ThemeForegroundBrush}"/>
                        <TextBlock x:Name="SubtitleTextBlock" Margin="0,8,0,0" FontSize="13" Foreground="{DynamicResource ThemeSubtleForegroundBrush}" TextWrapping="Wrap"/>
                    </StackPanel>

                    <Button Grid.Column="3"
                            x:Name="CloseButton"
                            Width="34"
                            Height="34"
                            Margin="12,0,0,0"
                            Click="CloseButton_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                         <Setter TargetName="Root" Property="Background" Value="{DynamicResource ThemeControlHoverBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="11" Foreground="{DynamicResource ThemeSubtleForegroundBrush}"/>
                    </Button>
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,22,0,0">
                    <Border Background="Transparent" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="LanguageLabelTextBlock" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource ThemeForegroundBrush}"/>
                            <TextBlock x:Name="LanguageHintTextBlock" Grid.Row="1" Margin="0,8,0,0" FontSize="12" Foreground="{DynamicResource ThemeSubtleForegroundBrush}" TextWrapping="Wrap"/>
                            <ComboBox Grid.Row="2"
                                      x:Name="LanguageComboBox"
                                      Margin="0,14,0,0"
                                      Height="36"
                                      Style="{StaticResource ModernComboBox}"
                                      DisplayMemberPath="DisplayName"
                                      SelectedValuePath="Language"/>
                        </Grid>
                    </Border>

                    <Border Background="Transparent" BorderThickness="0" Padding="0" Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock x:Name="UtilityLabelTextBlock" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource ThemeForegroundBrush}"/>
                            <TextBlock x:Name="UtilityHintTextBlock" Margin="0,10,0,0" FontSize="12" Foreground="{DynamicResource ThemeSubtleForegroundBrush}" TextWrapping="Wrap"/>

                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="180"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock x:Name="AutoSaveIntervalLabelTextBlock" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0,6"/>
                                <ComboBox x:Name="AutoSaveIntervalComboBox" Grid.Column="1" Height="32" Style="{StaticResource ModernComboBox}" Margin="0,3"/>

                                <TextBlock x:Name="PressureLabelTextBlock" Grid.Row="1" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0,6"/>
                                <CheckBox x:Name="PressureCheckBox" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"
                                          Foreground="{DynamicResource ThemeForegroundBrush}"
                                          Background="{DynamicResource ThemeSurfaceBrush}"
                                          BorderBrush="{DynamicResource ThemeBorderBrush}"/>

                                <TextBlock x:Name="SmoothingLabelTextBlock" Grid.Row="2" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0,6"/>
                                <ComboBox x:Name="SmoothingComboBox" Grid.Row="2" Grid.Column="1" Height="32" Style="{StaticResource ModernComboBox}" Margin="0,3"/>

                                <TextBlock x:Name="DefaultPenColorLabelTextBlock" Grid.Row="3" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0,6"/>
                                <TextBox x:Name="DefaultPenColorTextBox" Grid.Row="3" Grid.Column="1" Height="32" Padding="8,4" Text="#000000"
                                         Foreground="{DynamicResource ThemeForegroundBrush}"
                                         Background="{DynamicResource ThemeSurfaceAltBrush}"
                                         BorderBrush="{DynamicResource ThemeBorderBrush}"/>

                                <TextBlock x:Name="DefaultPenSizeLabelTextBlock" Grid.Row="4" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0,6"/>
                                <TextBox x:Name="DefaultPenSizeTextBox" Grid.Row="4" Grid.Column="1" Height="32" Padding="8,4" Text="1.5"
                                         Foreground="{DynamicResource ThemeForegroundBrush}"
                                         Background="{DynamicResource ThemeSurfaceAltBrush}"
                                         BorderBrush="{DynamicResource ThemeBorderBrush}"/>

                                <TextBlock x:Name="ThemeLabelTextBlock" Grid.Row="5" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0,6"/>
                                <ComboBox x:Name="ThemeComboBox" Grid.Row="5" Grid.Column="1" Height="32" Style="{StaticResource ModernComboBox}" Margin="0,3"/>

                                <TextBlock x:Name="PerformanceModeLabelTextBlock" Grid.Row="6" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0,6"/>
                                <ComboBox x:Name="PerformanceModeComboBox" Grid.Row="6" Grid.Column="1" Height="32" Style="{StaticResource ModernComboBox}" Margin="0,3"/>

                                <TextBlock x:Name="PenOnlyLabelTextBlock" Grid.Row="7" VerticalAlignment="Center" Foreground="{DynamicResource ThemeForegroundBrush}" Margin="0,6"/>
                                <CheckBox x:Name="PenOnlyCheckBox" Grid.Row="7" Grid.Column="1" VerticalAlignment="Center"
                                          Foreground="{DynamicResource ThemeForegroundBrush}"
                                          Background="{DynamicResource ThemeSurfaceBrush}"
                                          BorderBrush="{DynamicResource ThemeBorderBrush}"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
                    <Button x:Name="CancelButton" Click="CancelButton_Click" Style="{StaticResource DialogSecondaryButton}" Margin="0,0,10,0"/>
                    <Button x:Name="SaveButton" Click="SaveButton_Click" Style="{StaticResource DialogPrimaryButton}"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</Window>
