# Caelum V5 Tasks 23-40 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish and verify the remaining V5 checklist requirements, including the already-started Task 23-27 code paths, then synchronize `docs/tasks.md`, `docs/checklist.md`, and `.ai/`.

**Architecture:** Preserve the existing WPF single-window/Frame-tab architecture, `PdfPageControl` layered annotation surface, `PdfService` strip-and-rebuild PDF pipeline, and `EditorPage` `IUndoAction` command stack. Add new behavior behind small models/services where possible; keep PDF annotations owned by the existing DIP↔PDF-point conversion layer and use document snapshots for structural page operations.

**Tech Stack:** .NET 8 WPF, PdfiumViewer, PdfSharpCore 1.3.67, System.Text.Json, WPF `DispatcherTimer`/`Canvas`/`Popup`, NUnit.

---

### Task 23-24: Audit the existing implementation

**Files:** `Models/AppSettings.cs`, `Services/AppSettingsService.cs`, `Pages/EditorPage.xaml(.cs)`, `Controls/PdfPageControl.xaml.cs`, `docs/tasks.md`, `docs/checklist.md`

- [x] Verify preset capture/apply and smoothing propagation against the source; correct propagation and document the supported right-click capture behavior.
- [x] Mark verified subtasks complete; the full suite and build pass.

### Task 25: PDF text markup

**Files:** `Models/AnnotationModels.cs`, `Controls/PdfPageControl.xaml(.cs)`, `Pages/EditorPage.xaml(.cs)`, `Services/PdfService.cs`, `WindowsNotesApp.Tests/PdfServiceAnnotationSavingTests.cs`

- [x] Reuse the existing PDF text-selection rectangles and add explicit Underline, StrikeOut and Squiggly commands.
- [x] Persist each markup as `TextMarkupAnnotation`, render it in the overlay, route it through selection/undo, and round-trip the three owned standard subtypes.
- [x] Saving/parsing assertions pass in `PdfServiceAnnotationSavingTests`.

### Task 26-27: Sticky notes and area highlights

**Files:** `Controls/PdfPageControl.xaml(.cs)`, `Pages/EditorPage.xaml(.cs)`, `Services/PdfService.cs`, `WindowsNotesApp.Tests/PdfServiceAnnotationSavingTests.cs`

- [x] Wire `StickyNote` and `AreaHighlight` tool modes, activation/edit events, model collection, overlay editing, deletion, moving, and undo.
- [x] Preserve standard `/Text` notes and owned rectangular `/Highlight` annotations through save/load without stripping foreign annotations.
- [x] Add geometry/round-trip coverage and verify the new containers participate in selection and structural snapshots.

### Task 28: handwriting-to-text spike and supported fallback

**Files:** `Services/HandwritingRecognitionService.cs`, `Pages/EditorPage.xaml(.cs)`, `.ai/Services/HandwritingRecognitionService.md`, `docs/spec.md`, `docs/tasks.md`

- [x] Probe the Windows InkAnalyzer feasibility and record a deterministic unavailable result for the current WPF target without coupling the editor to a platform-only type.
- [x] Show a visible localized “conversion unavailable” fallback and record the degraded result in the spec; selected strokes remain untouched.

### Task 29: rich text

**Files:** `Models/AnnotationModels.cs`, `Pages/EditorPage.xaml(.cs)`, `Services/PdfService.cs`, `WindowsNotesApp.Tests/PdfServiceAnnotationSavingTests.cs`

- [x] Add `Bold`, `Italic`, `FontFamily`, and `Alignment` fields with defaults that keep old JSON readable.
- [x] Add an inline toolbar, one-step format undo, load restoration, and FreeText/CJK style propagation.

### Task 30: thumbnails and page reorder

**Files:** `Pages/EditorPage.xaml(.cs)`, `Models/PageInsertTemplate.cs`, tests for page snapshots

- [x] Add a collapsible left thumbnail panel populated by low-DPI page render APIs; update selected-page highlighting and click navigation.
- [x] Implement drag reorder with a before/after document snapshot action and expose page add/delete/copy context commands.

### Task 31: outline and bookmarks

**Files:** `Services/PdfService.cs`, `Services/BookmarkService.cs`, `Pages/EditorPage.xaml(.cs)`, `.ai/Services/BookmarkService.md`

- [x] Read outline titles/destinations into a UI tree and jump via the existing page navigation helper.
- [x] Store file-path keyed custom page bookmarks in `%LOCALAPPDATA%\\Caelum\\bookmarks.json`; wire Ctrl+M, add/remove, delete and jump.

### Task 32: full-text search

**Files:** `Pages/EditorPage.xaml(.cs)`, `Services/PdfService.cs`, tests for text extraction/search helpers

- [x] Add a search panel backed by cached `PdfPageTextInfo`, with page/snippet results and asynchronous refresh.
- [x] Wire result click, Enter/F3, Shift+F3, current-hit highlight, and Escape close without stealing text-box input.

### Task 33: keyboard completion

**Files:** `Pages/EditorPage.xaml(.cs)`, `MainWindow.xaml(.cs)`, tests for navigation helpers

- [x] Add PageUp/PageDown/Home/End and Ctrl+A current-page annotation selection with text-input guards.
- [x] Add Ctrl+Tab/Ctrl+Shift+Tab cycling through the existing `_tabs` list while preserving the active Frame.

### Task 34: fit zoom and page rotation

**Files:** `Pages/EditorPage.xaml(.cs)`, `Services/PdfService.cs`, `Controls/PdfPageControl.xaml(.cs)`

- [x] Compute fit-width/fit-page zoom from the viewport and existing page DIP sizes.
- [x] Rotate the selected page by 90 degrees with a before/after snapshot action and preserve `/Rotate` through save/reload.

### Task 35: page templates

**Files:** `Models/PageInsertTemplate.cs`, `Services/PdfService.cs`, `PageTemplatePickerWindow.xaml(.cs)`, `Pages/EditorPage.xaml(.cs)`, `.ai/Models/PageInsertTemplate.md`

- [x] Add Dotted, Music and Cornell enum values, picker cards, labels and vector PDF drawing.
- [x] Add template regression tests through blank-page creation/insertion.

### Task 36: PNG export and dead-code removal

**Files:** `Pages/EditorPage.xaml(.cs)`, `SimplePdfExporter.cs`, `docs/tasks.md`

- [x] Export the current page or all pages at 1x/2x with annotations through the existing printable render APIs and write PNG files selected by the standard dialogs.
- [x] Delete `SimplePdfExporter.cs` after confirming zero project references and run a clean build.

### Task 37: insert PDF/image pages

**Files:** `Pages/EditorPage.xaml(.cs)`, `Services/PdfService.cs`, `Models/PageInsertTemplate.cs`

- [x] Add PDF source plus inclusive page-range selection and copy source pages into the current document.
- [x] Add image-to-new-page insertion with aspect-fit centering; wrap both operations in before/after snapshots for undo.

### Task 38: settings expansion and auto-save errors

**Files:** `Models/AppSettings.cs`, `Services/AppSettingsService.cs`, `SettingsWindow.xaml(.cs)`, `Pages/EditorPage.xaml(.cs)`, `Services/LocalizationService.cs`, `.ai/` mirrors

- [x] Add auto-save interval, default pen color/size, smoothing and theme fields; sanitize/clone every field and apply changes immediately.
- [x] Reconfigure the active timer when the interval changes and show a visible error toast when auto-save fails.

### Task 39: dark theme

**Files:** `App.xaml`, `App.xaml.cs`, `MainWindow.xaml(.cs)`, `Pages/HomePage.xaml(.cs)`, `Pages/EditorPage.xaml(.cs)`, `SettingsWindow.xaml(.cs)`, `Models/AppSettings.cs`

- [x] Add light/dark resource brushes, apply the selected theme to app chrome and editor backgrounds without tinting PDF pages, and persist the selection.
- [x] Validate resource lookup and primary-window contrast at code level; remaining interactive visual checks are recorded as environment-dependent.

### Task 40: final verification and documentation

**Files:** `docs/tasks.md`, `docs/checklist.md`, `.ai/PROJECT_CONTEXT.md`, all touched `.ai` mirrors

- [x] Run the solution build and full `WindowsNotesApp.Tests` suite; retain the final result and fix all errors/failures.
- [x] Perform the available code-level/manual regression checklist, record environment-limited checks honestly, and synchronize every task checkbox and mirror open thread/change history.

## Completion record (2026-08-19)

- `dotnet build Windows-Notes.sln --no-restore`: 0 errors, 5 known warnings (PdfiumViewer compatibility and WFAC010 high-DPI metadata).
- `dotnet test WindowsNotesApp.Tests --no-restore`: 29/29 passing after the page-editing regression tests.
- Task 28 is intentionally a visible fallback because the current project has no WinRT InkAnalyzer projection; selected strokes are preserved.

**Checkpoints:** after Tasks 25-27, after Tasks 29-34, after Tasks 35-39, and before Task 40 documentation sync.
