# RecentFilesServiceLibrarySurvivalTests

## Purpose

Proves the library index keeps missing PDFs, restores from `recent_files.json.bak` / bookmark paths, and names file tiles from the path when `DisplayName` is empty.
