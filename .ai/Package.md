# Package.appxmanifest
> Last updated: 2026-08-24 | Protection: STANDARD

## Purpose

Preserves the legacy Windows package identity and version metadata for compatible OpenNotes upgrades.

## Important Notes / NEVER Change

- Identity name remains `WindowsNotesApp`; publisher remains `CN=Learnmore_smart`.

## Open Threads / Resume Context

- **Status:** released (5.2.9 Edge-PDF compatibility release)
- Package version is `5.2.9.0`; tag `v5.2.9` published both verified GitHub assets while `WindowsNotesApp` and `CN=Learnmore_smart` remained unchanged.
- **Status:** released (5.2.7 page-rotation drawing hotfix)
- Package version is `5.2.7.0`; `WindowsNotesApp` and `CN=Learnmore_smart` remain unchanged.
- **Status:** ready_for_release (5.2.0 feature release)
- Only the package version changed to `5.2.0.0`; `WindowsNotesApp` and `CN=Learnmore_smart` remain unchanged.
- **Status:** complete (5.1.2 patch release)
- Only the package version changed to `5.1.2.0`; identity and publisher remain unchanged.
- **Status:** complete
- Identity remains unchanged; only the version was aligned to `5.0.1.0` for the startup-crash patch release.
